#include<iostream>
#include<string>
using namespace std;

int main()
{
	int a,b;
	string result;
	char s[100];
	while(cin>>a>>b)
	{
		if(a==0&&b==0)
		{
			break;
		}
		sprintf(s,"%d",a+b);
		result+=s;
		result+="\n";	
	}
	cout<<result;
}
